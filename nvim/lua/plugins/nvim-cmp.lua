return {
  "hrsh7th/nvim-cmp",
  opts = {
    completion = {
      keyword_length = 3, -- require 2 characters before showing completions
    },
    preselect = "none", -- don't preselect first item
  },
}
